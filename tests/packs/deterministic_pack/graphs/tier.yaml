id: tier
description: Classify an amount into a refund tier.
inputs:
  amount: float | None
outputs:
  tier: Literal["high", "low"]
state:
  amount: float | None
start: decide
nodes:
  decide:
    type: router
    edges:
      state.amount > 100: high
    default: low

  high: { type: end, outputs: { tier: high } }
  low:  { type: end, outputs: { tier: low } }
